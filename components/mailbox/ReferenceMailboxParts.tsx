// Compatibility for older imports. All artwork is now complete PNG sprites;
// there are no reference-image crops, donor patches, or hinge overlays.
export { MailboxShell, MailboxDoor, MailboxFlag } from './MailboxParts';
